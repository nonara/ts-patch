const fs = require('fs');
const { tmpDir } = require('./config');


/* ****************************************************************************************************************** *
 * Teardown
 * ****************************************************************************************************************** */

module.exports = () => {
  if (fs.existsSync(tmpDir)) (fs.rmSync || fs.rmdirSync)(tmpDir, { recursive: true });
}
